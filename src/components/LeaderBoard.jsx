import React from "react";
import { Typography } from "@mui/material";

const LeaderBoard = ({ level, data }) => {
  return (
    <Box>
      <Button></Button>
      <Typography>
        <Box></Box>
      </Typography>
    </Box>
  );
};

export { LeaderBoard };
